# AskLens 面试自测清单

> **用法**：学完对应阶段后，不看笔记、不看代码，口头回答以下问题。
> 能流畅回答的 ✅，需要翻笔记的 ⚠️，完全答不上来的 ❌——❌ 多的模块回头重学。

---

## V1 认证与群组

- [ ] 单令牌 vs 双令牌：各有什么优缺点？Access Token 为什么设 15min 而不是 1 小时？
- [ ] Refresh Token Rotation 是什么？没有 Rotation 会有什么安全风险？
- [ ] `JwtAuthenticationFilter` 在 Spring Security 过滤器链中的位置？为什么放那个位置？
- [ ] `CurrentUserService.getRequiredCurrentUser()` 的实现原理——怎么从 HttpServletRequest 拿到用户信息？
- [ ] 群组权限校验的 SQL 长什么样？为什么不能只查 `group` 表？
- [ ] 如果一个用户不属于某群组，ta 能通过直接调 API 看到该群组的文档吗？哪一层拦截的？

---

## V2 文档引擎

### 分片上传

- [ ] 分片上传的三阶段是哪三个阶段？每个阶段的职责？
- [ ] 断点续传怎么实现的？上传会话存在哪里？过期时间多少？
- [ ] 秒传（SHA-256）的流程：怎么判断文件已存在？如果同一个文件被两个不同群组上传，会创建两份存储对象吗？
- [ ] 如果用户并发上传同一个文件两次，系统怎么处理？
- [ ] MinIO 的 `composeObject` 做了什么？为什么合并操作不需要经过应用服务器？
- [ ] 为什么选择 MinIO 而不是本地磁盘或直接 AWS S3？三个理由。
- [ ] `ObjectStorageService` 接口有几个方法？如果 MinIO 没配，`MissingObjectStorageService` 的行为是什么？

### ETL 流水线

- [ ] ETL 7 步分别做什么？顺序能调换吗？
- [ ] ETL 是同步还是异步？如果是同步会有什么后果？
- [ ] Spring Event 怎么触发 ETL？从上传完成到 ETL 开始，经过了哪些组件？
- [ ] `@Retryable` 的参数是什么（maxAttempts、backoff）？重试失败后 `@Recover` 做了什么？
- [ ] 如果没有 `@Recover` 兜底，ETL 失败后文档会卡在什么状态？
- [ ] ETL 为什么用轮询 + 状态机而不是消息队列？各有什么优劣？

### 设计模式

- [ ] `DocumentParserFactory` 用了什么设计模式？为什么不用 if-else？
- [ ] 如果要加一种新格式（如 HTML），需要改几个文件？每个改什么？
- [ ] `LinkedHashMap` 在这里的作用是什么？换成 `HashMap` 有问题吗？
- [ ] `DocumentUploadService` 的 Template Method 模式体现在哪里？
- [ ] `ObjectStorageService` 接口 + `@ConditionalOnProperty` 体现了什么设计原则？

### 结构感知切片

- [ ] 固定长度切片的最大问题是什么？举一个具体的例子说明。
- [ ] 结构感知切片的四级降级策略是什么（按优先级排序）？
- [ ] 代码块保护是怎么实现的？为什么需要在切片阶段考虑代码块？
- [ ] `sectionPath` 元数据存了什么？对后续检索有什么帮助？
- [ ] greedy merge 的策略是什么？解决了什么问题？
- [ ] 切片参数（targetTokens、maxTokens、overlapTokens）的默认值是多少？调大 overlap 会怎样？

### 双索引

- [ ] PGvector 用什么索引算法？距离度量选了什么？为什么？
- [ ] ES 用了什么分词器？两阶段打分（bool + rescore）各自做什么？
- [ ] ES 不可用时检索行为是什么？是抛异常还是返回空？去源码找到具体的降级代码在哪一行。
- [ ] 向量检索的结果和关键词检索的结果，分数可以直接比较吗？为什么？

---

## V3 RAG 问答

### 全流程

- [ ] 画出 QA 8 阶段流程图（不查文档）。
- [ ] 每个阶段输入什么、输出什么？失败时的兜底策略是什么？
- [ ] 整个流程中，哪些步骤调了 LLM？各调了几次？

### 查询规划

- [ ] DIRECT / REWRITE / DECOMPOSE 各适合什么场景？举出具体问题例子。
- [ ] DECOMPOSE 最多生成几条子查询？为什么加这个限制？
- [ ] 查询规划失败（LLM 返回的 JSON 解析不了）时的 fallback 是什么？
- [ ] 如果 LLM 返回了 REWRITE 策略但没有给出 rewrittenQuery，代码会怎么处理？
- [ ] 多查询是并行执行还是串行执行？去源码验证你的答案。

### RRF 融合

- [ ] RRF 公式写出来，解释每个符号的含义。
- [ ] k 参数默认是多少？调大 k 会让融合结果更"平等"还是更"精英"？
- [ ] 为什么 RRF 比加权分数融合更鲁棒？至少两个理由。
- [ ] 如果向量检索的结果排序和关键词检索完全相反，RRF 会怎么处理？
- [ ] RRF 在什么情况下会失效？

### 邻居窗口 + 类簇聚合

- [ ] 邻居窗口为什么是 ±1 而不是 ±2？如果答案跨了 3 个切片，±1 够吗？
- [ ] 类簇聚合怎么判断两个切片是"连续的"？用什么字段？
- [ ] 邻居窗口和类簇聚合的执行顺序？先扩展再聚合，还是反过来？

### 证据评估

- [ ] 四级证据等级的定义和对应的系统行为。
- [ ] 证据不足时为什么选择拒答而不是勉强回答？这里的产品权衡是什么？
- [ ] NONE 和 WEAK 的判定阈值是怎么区分的？代码里怎么计算证据充分度？
- [ ] 如果评估结果是 WEAK，LLM 的 System Prompt 会怎么变？

### 引用溯源

- [ ] `CitationAssembler` 是怎么把 LLM 的输出和检索结果关联起来的？
- [ ] citations 里包含哪些字段？前端可以怎么展示？

### 综合

- [ ] 如果检索结果全是质量很低的切片（分数都 < 0.3），问答流程会怎样？
- [ ] 如果用户问"总结一下我上传的所有文档"，这个系统能回答吗？为什么？
- [ ] V3 QA 模块为什么不保存对话历史？这有什么工程上的考量？

---

## V4 AI Agent

- [ ] QA 模块和 Assistant 模块的核心差异（架构层面，不是功能列表）。
- [ ] 为什么"简单查询走 QA，复杂对话走 Agent"是合理的分层？
- [ ] ReactAgent 的「思考 → 工具调用 → 生成」循环：Agent 怎么决定是否调用工具？
- [ ] `AssistantKnowledgeBaseTool` 内部复用了 V3 的哪个类？为什么能直接复用？
- [ ] 为什么限制"每轮最多一次工具调用"？不限制会有什么问题？
- [ ] 三级记忆压缩的触发条件和压缩策略。
- [ ] SSE 流式输出的 Delta 去重是怎么做的？为什么需要去重？
- [ ] CHAT 和 KB_SEARCH 模式切换时，Agent 的行为有什么不同？
- [ ] Agent 的 System Prompt 和 QA 的 System Prompt 有什么本质区别？

---

## 跨阶段综合题

- [ ] 从用户上传一个 PDF 到用户提问得到回答，数据经过了哪些存储系统（表/索引/引擎）？画出完整的数据流向图。
- [ ] 如果让你把 PGvector 换成 Milvus，需要改哪些文件？`PgVectorRetrievalAdapter` 以外还需要动什么？
- [ ] 如果让你加 Reranker（Cross-Encoder 重排），你会插在 8 阶段的哪一步之后？为什么？
- [ ] 整个项目里，`groupId` 过滤出现在哪些 SQL / 检索请求中？如果漏掉一个会有什么后果？
- [ ] 这个项目的异常处理体系：有哪些自定义异常？`GlobalExceptionHandler` 怎么把异常映射到 HTTP 状态码？
- [ ] 如果要支持多语言文档（英文 + 中文混合），切片和检索各需要做哪些调整？

---

## 开放设计题（没有标准答案，但面试可能问）

- [ ] 如果不允许使用 LLM 做查询规划（成本考量），你会用什么替代方案？
- [ ] 如果用户量增长 10 倍，ETL 异步轮询可能成为瓶颈——你会怎么改造？
- [ ] QA 模块现在没有会话持久化——你会怎么设计会话表？历史对话如何影响新一轮检索？
- [ ] 如果用户上传了一个 200 页的 PDF，切片后可能有 500+ 个 chunk——检索和 RRF 的性能怎么保证？
