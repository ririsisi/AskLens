# AskLens 学习笔记模板

> **用法**：每学完一个模块，用自己的话填写下面的空白——**不要复制粘贴文档或 AI 的回答**。
> 面试时你只能靠自己的理解，现在写的每一个字都是面试时的弹药。

---

## 已完成 ✅ V1 工程底座

**我理解的核心要点**（3-5 句话）：

> （你的话）

**双令牌设计的 WHY**：

> （你的话：解决了什么问题？为什么不用单令牌？Refresh Token Rotation 的必要性？）

**群组权限模型如何影响后续模块**：

> （你的话：为什么 groupId 过滤会贯穿 V2/V3/V4 的数据库查询？）

**还不确定的疑问**：

> 

---

## V2 文档引擎

### 2.1 分片上传

**三阶段协议我理解是什么**：

> （用自己的话描述 init → chunk → complete）

**解决了哪 5 个问题**（不查文档，默写）：

> 1.
> 2.
> 3.
> 4.
> 5.

**MinIO vs 本地磁盘，我最信服的理由是**：

> 

**composeObject 为什么不需要经过应用服务器**：

> 

**关键源码路径**（你实际跟过的）：

> Controller: `______`
> Service: `______`
> 状态存哪: `______` 表

---

### 2.2 ETL 7 步流水线

**7 步分别做什么**（默写）：

| 步骤 | 做什么 | 失败后果 |
|------|--------|---------|
| 1. | | |
| 2. | | |
| 3. | | |
| 4. | | |
| 5. | | |
| 6. | | |
| 7. | | |

**异步机制怎么工作的**：

> （你的话：Spring Event → Listener → @Async → @Retryable → @Recover）

**为什么要异步而不是同步**：

> 

**@Recover 的作用是什么，没有它会怎样**：

> 

---

### 2.3 工厂模式 + 结构感知切片

**DocumentParserFactory 怎么避免 if-else**：

> 

**如果不用工厂模式，直接 if-else，加第 5 种格式会多改几个地方**：

> 

**结构感知切片的四级降级**（默写）：

> 1. 第一优先：
> 2. 第二优先：
> 3. 第三优先：
> 4. 兜底：

**和固定长度切片的对比**（至少说出 3 点差异）：

> 1.
> 2.
> 3.

**chunk 的元数据里存了什么，为什么重要**：

> 

---

### 2.4 双索引写入

**PGvector 和 ES 各擅长什么**：

| | PGvector | Elasticsearch |
|---|---------|---------------|
| 擅长 | | |
| 不擅长 | | |

**ES 挂了怎么办**（去源码查降级逻辑）：

> 

---

### V2 面试话术草稿

**分片上传**（30 秒版本）：

> 

**ETL 异步化**（30 秒版本）：

> 

**结构感知切片**（30 秒版本）：

> 

---

## V3 RAG 问答

### 3.1 8 阶段全流程（默写）

```
1.
2.
3.
4.
5.
6.
7.
8.
```

**每个阶段失败时的兜底是什么**：

| 阶段 | 失败兜底 |
|------|---------|
| 查询规划 | |
| 双通道检索 | |
| RRF 融合 | |
| 证据评估 | |
| LLM 生成 | |

---

### 3.2 查询规划

**三种策略 + 适用场景**（不查文档）：

| 策略 | 含义 | 适用场景 |
|------|------|---------|
| DIRECT | | |
| REWRITE | | |
| DECOMPOSE | | |

**DECOMPOSE 最多生成几条检索语句，为什么限制**：

> 

**多查询是怎么执行的**（去源码查：并行还是串行？）：

> 

---

### 3.3 RRF 融合

**RRF 公式默写**：

```
RRF Score(d) =
```

**k 参数的含义**：

> 

**为什么 RRF 比加权分数融合更鲁棒**（至少 2 个理由）：

> 1.
> 2.

**如果只有向量检索没有 ES（或反过来），RRF 行为会变吗**：

> 

---

### 3.4 邻居窗口 + 类簇聚合

**邻居窗口解决了什么问题**：

> 

**类簇聚合解决了什么问题**：

> 

---

### 3.5 证据评估 + 拒答

**四级证据等级**（默写）：

| 等级 | 含义 | 行为 |
|------|------|------|
| | | |
| | | |
| | | |
| | | |

**为什么这是企业 RAG 和 Demo RAG 的核心差异**：

> 

**如果让你决定 NONE 和 WEAK 的阈值，你会怎么定**：

> 

---

### 3.6 引用溯源

**CitationAssembler 输出的 citations 包含哪些字段**：

> 

---

### V3 面试话术草稿

**查询规划**（30 秒版本）：

> 

**RRF**（30 秒版本）：

> 

**证据评估**（30 秒版本）：

> 

---

## V4 AI Agent

### 4.1 QA vs Assistant

**核心差异**（不只是"一个是流水线一个是 Agent"——说清楚架构选择）：

| 维度 | QA | Assistant |
|------|-----|-----------|
| 决策方式 | | |
| 检索时机 | | |
| 对话能力 | | |
| 延迟特性 | | |

**什么时候该用 QA，什么时候该用 Assistant**：

> 

---

### 4.2 Agent 核心机制

**ReactAgent 循环**（用自己的话描述）：

> 

**AssistantKnowledgeBaseTool 怎么复用 V3 的检索**：

> 

**为什么限制每轮最多一次工具调用**：

> 

**三级记忆压缩**：

| 级别 | 做什么 | 触发条件 |
|------|--------|---------|
| L1 | | |
| L2 | | |
| L3 | | |

---

### V4 面试话术草稿

**Agent 设计**（30 秒版本）：

> 

---

## 全局面试准备

**如果面试官说"讲一下你在 AskLens 项目里做了什么"**（2 分钟版本）：

> 

**如果面试官问"你在这个项目里遇到的最大挑战是什么"**（1 分钟版本）：

> 

**如果面试官问"你的 RAG 和 LangChain/LlamaIndex 套个 Demo 有什么区别"**（1 分钟版本）：

> 

**我还有疑问、但暂时没时间深究的点**：

> 
> 
> 
