# 学习进度

> 导师每次开课先读这里，下课后回写这里。状态：✅ 已掌握 / 🟡 学过待巩固 / ⬜ 未开始。

## 当前位置

- **上次学到**：V3 模块 3.3 后处理 —— **类簇聚合已讲透**（聚簇=同文档「严格 +1 连续」切片并成一簇，一簇=一条证据；先聚簇再整段 ±1 扩展去重，天然不重复发；严格 +1 而非「相近就并」是「精确率 vs 完整性」取舍——宁可偶尔重复一小片，也不把检索判定为无关的中间片塞进证据；学生自己揪出「#5/#7 命中、#6 漏，两条独立证据各自 ±1 会让 #6 重复一次」的边界）。四级证据评估在「面试模拟」里开了头。
- **今天/下次从这里接**：**四级证据评估的两个追问（学生未答即下课，已入复习队列）**：① 判据是「分数 + 条数 + **命中来源 BOTH**」三维度，学生漏了「来源」维度（SUFFICIENT 要求 ≥2 条且 BOTH 或 向量+归一化分≥0.85；两路都中=两套独立机制互相印证更可信）；② 等级如何改变行为——**不是 Java 硬分支，而是 `buildEvidenceGuidance` 把等级翻译成「回答指导语」注入 prompt**，WEAK=谨慎答非拒答，拒答只在 NONE。下次先冷考这两点，再往后讲：拒答逻辑 → 引用溯源（CitationAssembler 按文件名去重）。
- **最近一次学习日期**：2026-06-24

## 总览（对应 `docs/AI-学习引导提示词.md` 大纲）

### V1 工程底座（认证与群组）
- ✅ 整体（已完成，建议用「复习」命令做一次快速回顾检验）

### V2 文档引擎
- ✅ 整体（据 learner-profile 标记为已完成；建议日后用「复习」抽查 2.3 切片四级降级 + 2.4 双索引降级）

### V3 RAG 问答
- ✅ 3.1 8 阶段全流程 + 查询规划（DIRECT / REWRITE / DECOMPOSE）
- ✅ 3.2 RRF 融合
- 🟡 3.3 后处理（邻居窗口 ✅、类簇聚合 ✅ 已讲透；四级证据评估 🟡 面试模拟开头、两追问待巩固；拒答 / 引用溯源 未讲）
- ⬜ 3.4 综合验证

### V4 AI Agent
- ⬜ 4.1 ReactAgent vs QA 固定流水线
- ⬜ 4.2 工具复用、单轮单工具、三级记忆压缩、SSE 去重

---

## 课堂日志（最新在上）

> 格式：日期 | 模块 | 学到了什么 / 下次注意

- 2026-06-24 | V3 3.3 类簇聚合 + 四级证据(面试模拟) | **热身**：RAG 8 阶段顺序冷考答对（RRF 在聚簇/窗口之前，因果讲清「聚簇要靠融合后的统一排名定簇代表」），🔴老毛病这次纠回，待再答对一次归档。**类簇聚合**讲透：一簇=一条证据、先聚簇再整段 ±1 扩展去重、`isContinuousWith` 严格 +1（`endChunkIndex+1`）、严格 vs 放宽的取舍（举退款/配送费例子说明放宽会把无关 #6 钉进证据正中央稀释质量）；学生自己推出 #5/#7 命中→#6 漏→两条证据各自 ±1 让 #6 重复的边界。**话术**：帮他把类簇聚合校准成「痛点(重复发送+虚假证据强度)→解法(聚簇)→亮点(严格+1取舍/宁可重复不引噪声)」结构版（见下）。**面试模拟**：四级证据评估——四级名字答对(NONE/WEAK/PARTIAL/SUFFICIENT)、动作大方向对，但①判据漏「命中来源 BOTH」维度 ②不懂「等级翻译成 guidance 注入 prompt、WEAK≠拒答」机制，两点未答即下课，已入复习队列。**注意点**：输出/面试模拟开口仍是「我做了 ABC」平铺，未自发用「痛点开场」结构，🟡 话术结构问题复发。
  - 📌 类簇聚合 30 秒话术（痛点→解法→亮点）：「直接喂切片有两个坑——同文档相邻切片各自做窗口扩展会重复发送，既烧 token 又让模型误以为多条证据印证、制造虚假证据强度。所以我加了类簇聚合：把同文档序号严格连续的切片先并成一簇、整段一次性扩展去重，一簇对外只算一条证据。关键是『严格相邻才并』而非『相近就并』——中间那片没被任何通道命中恰恰说明它无关，强行并进来会把无关内容塞进证据正中央稀释质量；代价只是偶尔重复一小片邻居。我宁可重复一小片，也不把检索判定为无关的内容当证据，本质是精确率优先的取舍。」
- 2026-06-15(下午) | V3 3.2 RRF + 3.3 开头 | 学了：双路分数值域不可比（向量 0~1 / BM25 0~∞），直接相加退化成纯关键词检索；RRF 用排名（scale 无关）而非原始分，公式 Σ 1/(k+rank)；AskLens 取 k=0（非默认 60）拉开头部区分度，因为融合分身兼二职——既排序又当可信度，配 1-e^(-x) 归一化 + 0.85 证据阈值（k=60 会压平分数够不到阈值）。3.3：邻居窗口 ±1 恢复被 token 截断的语义，±1 是「完整性 vs token 成本 vs 噪声」+ 32 token overlap 的甜点位。表现：3.2 的 k=0 因果链能独立讲出来；但热身复述 8 阶段顺序又把 RRF/聚簇记反（靠反例纠回）；面试话术仍倾向先抛公式后讲痛点，需练「痛点→解法→亮点」结构。
- 2026-06-15 | V3 3.1 查询规划 | 学了：RAG 8 阶段流程；查询规划三策略 DIRECT/REWRITE/DECOMPOSE（REWRITE 治指代模糊、DECOMPOSE 治一句多问）；规划失败静默降级 DIRECT 的「do no harm」容错；多子查询串行执行 + MAX_QUERY_COUNT=3 锁上限规避并发复杂度（共享 LinkedHashMap 的结构层 + 累加层竞态）。下次注意：8 阶段顺序里「RRF 融合排序在聚簇/邻居窗口之前」，别记反；面试话术要聚焦被问的点 + 讲 WHY，别平铺全流程。
